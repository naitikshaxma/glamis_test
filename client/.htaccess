<IfModule mod_rewrite.c>
    Options -MultiViews
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.html [QSA,L]

    # Ensure that the `dist` folder and its files are accessible
    # RewriteCond %{REQUEST_FILENAME} !-d
</IfModule>