{
  "name": "glamis",
  "version": "1.0.0",
  "description": "This is a mock interview portal designed for the students of GLA University to prepare for interviews.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd backend && npm i && npm run dev\" \"cd client && npm i && npm run dev\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
